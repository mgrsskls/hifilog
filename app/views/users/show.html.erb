<%= render layout: "users/shared/layout" do %>
  <%= render "shared/products",
    user: @user,
    items: @possessions,
    reset_path: @reset_path,
    hide_since: @render_period,
    hide_period: @render_since,
    hide_release_date: true,
    empty_state: t('empty_state.public_profile', name: h(@user.user_name)),
    include_images: true,
    items_breakpoint: 's',
    heading: @heading
  %>
<% end %>
