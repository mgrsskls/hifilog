<%# locals: brand, follows, count %>
<%= render "shared/people_preview",
            heading: t('brand_follow.followers_heading'),
            people: follows.map(&:user),
            count_label: t('brand_follow.followers_count', count:),
            empty_label: t('brand_follow.empty_state.no_followers') %>
