<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= APP_NAME %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="preload" href="/fonts/bungee.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="/fonts/satisfy.woff2" as="font" type="font/woff2" crossorigin>
    <%= yield :js %>
    <%= javascript_include_tag "theme_toggle", defer: true %>
    <%= stylesheet_link_tag "application" %>
    <%= yield :css %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="description" content="<%= strip_tags t('home.intro', products_size: app_size[:products].size, products_path: products_path, brands_size: app_size[:brands].size, brands_path: brands_path, categories_size: app_size[:categories].size, categories_path: categories_path).squish %>">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/svg" href="/favicon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
  </head>

  <body>
    <header>
      <div class="Header u-container">
        <a class="Skip" href="#content"><%= t('skip_to_main_content') %></a>
        <% if @is_home %>
          <h1 class="Menu-home">
            <span>HiFi</span><span>log</span>
          </h1>
        <% else %>
          <a class="Menu-home" href="<%= root_url %>"<% if current_page?(root_path) %> aria-current="page"<% end %>>
            <span>HiFi</span><span>log</span>
          </a>
        <% end %>
        <button type="button" aria-label="Menu" aria-controls="menu" aria-expanded="false" class="Header-toggle" onclick="this.setAttribute('aria-expanded', this.getAttribute('aria-expanded') === 'true' ? 'false' : 'true')">
          <svg class="Header-toggleIcon Header-toggleIcon--open" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="24" height="24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
          <svg class="Header-toggleIcon Header-toggleIcon--close" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="24" height="24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
        <div class="Header-menuContainer" id="menu">
          <div class="Header-menu">
            <nav aria-label="Main" class="Menu">
              <ul class="Menu-list Menu-list--main">
                <li>
                  <a href="<%= products_path %>"<%= active_menu_state(@active_menu, :products) %>>
                    <%= t('headings.products') %>
                  </a>
                </li>
                <li>
                  <a href="<%= brands_path %>"<%= active_menu_state(@active_menu, :brands) %>>
                    <%= t('headings.brands').html_safe %>
                  </a>
                </li>
                <li>
                  <a href="<%= categories_path %>"<%= active_menu_state(@active_menu, :categories) %>>
                    <%= t('headings.categories').html_safe %>
                  </a>
                </li>
              </ul>
            </nav>
            <search class="Header-search">
              <%= form_tag search_path, method: :get, class: "Search" do %>
                <%= text_field_tag :query, nil, type: 'search', value: '', class: 'Search-input', placeholder: t('search') + " …", minlength: 1, required: true %>
                <%= button_tag 'Search', class: 'Search-button', name: nil, "aria-label": t('search') do %>
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" width="20" height="20" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
                  </svg>
                <% end %>
              <% end %>
            </search>
          </div>
          <nav aria-label="Account">
            <ul class="Menu-list">
              <% if user_signed_in? %>
                <li>
                  <a href="<%= dashboard_products_path %>"<%= active_menu_state(@active_menu, :dashboard) %>>
                    <%= t('dashboard') %>
                  </a>
                </li>
                <li>
                  <%= button_to t('user_form.logout'), destroy_user_session_path, method: :delete  %>
                </li>
              <% else %>
                <li>
                  <a href="<%= new_user_registration_path %>"<%= active_menu_state(@active_menu, :signup) %>>
                    <%= t('user_form.signup') %>
                  </a>
                </li>
                <li>
                  <a href="<%= new_user_session_path %>"<%= active_menu_state(@active_menu, :login) %>>
                    <%= t('user_form.login') %>
                  </a>
                </li>
              <% end %>
            </ul>
          </nav>
        </div>
      </div>
    </header>
    <% unless render_breadcrumbs.empty? %>
      <nav aria-label="Breadcrumbs" class="Breadcrumbs">
        <ol class="Breadcrumbs-list u-container"><%= render_breadcrumbs :tag => :li, :separator => "" %></ol>
      </nav>
    <% end %>
    <main id="content">
      <div class="u-container">
        <%= render "shared/flash_messages", messages: flash if flash.any? %>
        <%= yield %>
      </div>
    </main>
    <footer>
      <% if newest_products.any? or newest_brands.any? %>
        <div class="Newest is-dark">
          <div class="u-container">
            <h2><%= t('home.recently_added') %></h2>

            <div class="Newest-lists u-flex u-flexWrap">
              <% if newest_products.any? %>
                <div>
                  <h3><%= t('headings.products') %></h3>
                  <ol>
                    <% newest_products.each do |product| %>
                      <li>
                        <time datetime="<%= formatted_date product.created_at %>"><%= formatted_date product.created_at %></time>:
                        <%= link_to product.brand.name + " " + product.name, brand_product_path(id: product.friendly_id, brand_id: product.brand.friendly_id) %>
                      </li>
                    <% end %>
                  </ol>
                </div>
              <% end %>
              <% if newest_brands.any? %>
                <div>
                  <h3><%= t('headings.brands') %></h3>
                  <ol>
                    <% newest_brands.each do |brand| %>
                      <li>
                        <time datetime="<%= formatted_date brand.created_at %>"><%= formatted_date brand.created_at %></time>:
                        <%= link_to brand.name, brand_path(id: brand.friendly_id) %>
                      </li>
                    <% end %>
                  </ol>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
      <div class="Footer">
        <div class="u-container u-flex u-spaceBetween u-alignCenter u-gap-md">
          <a href="https://www.github.com/mgrsskls/hifi-gear">Source Code</a>
          <theme-toggle>
            <div class="ThemeToggle">
              <input class="u-hiddenVisually" type="radio" name="theme" value="auto" id="theme-auto" checked="">
              <label for="theme-auto">
                <span class="u-hiddenVisually">Automatic</span>
                <svg class="ThemeToggle-icon ThemeToggle-icon--auto" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17 9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2Z"></path>
                </svg>
              </label>
              <input class="u-hiddenVisually" type="radio" name="theme" value="light" id="theme-light">
              <label for="theme-light">
                <span class="u-hiddenVisually">Light</span>
                <svg class="ThemeToggle-icon ThemeToggle-icon--light" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364-.707-.707M6.343 6.343l-.707-.707m12.728 0-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z"></path>
                </svg>
              </label>
              <input class="u-hiddenVisually" type="radio" name="theme" value="dark" id="theme-dark">
              <label for="theme-dark">
                <span class="u-hiddenVisually">Dark</span>
                <svg class="ThemeToggle-icon ThemeToggle-icon--dark" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none">
                  <path d="m20.354 15.354.928.373a1 1 0 0 0-1.3-1.3l.372.927ZM8.646 3.646l.928.373a1 1 0 0 0-1.301-1.301l.373.928Zm11.335 10.78c-.92.37-1.925.574-2.981.574v2a9.976 9.976 0 0 0 3.727-.718l-.746-1.856ZM17 15a8 8 0 0 1-8-8H7c0 5.523 4.477 10 10 10v-2ZM9 7c0-1.056.204-2.061.574-2.981l-1.856-.746A9.975 9.975 0 0 0 7 7h2Zm-5 5a8.003 8.003 0 0 1 5.019-7.426l-.746-1.856A10.003 10.003 0 0 0 2 12h2Zm8 8a8 8 0 0 1-8-8H2c0 5.523 4.477 10 10 10v-2Zm7.426-5.019A8.003 8.003 0 0 1 12 20.001v2c4.207 0 7.805-2.599 9.282-6.274l-1.856-.746Z"></path>
                </svg>
              </label>
            </div>
          </theme-toggle>
        </div>
      </div>
    </footer>
    <script>
      document.querySelectorAll("time").forEach(time => time.textContent = new Date(time.dateTime).toLocaleDateString())
    </script>
  </body>
</html>
